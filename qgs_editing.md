# QGISでラベルを修正して図面を書き出す

`qgs` フォルダの各QGZは、同じフォルダの `ringyo_zumen.gpkg` を参照します。
図面ごとに表示設定・レイアウト・ラベル位置を管理します。全体図でのラベル移動は、個別図には反映されません。

## ラベル位置を修正する

1. 修正したい図面の `.qgz` をQGISで開きます。
2. 地図画面で対象レイヤを選び、「ラベルツールバー」を表示します。
3. 「ラベルの移動」でラベルをドラッグします。レイアウト上ではなく、通常の地図画面で操作します。
4. 「プロジェクト」→「レイアウト」から同名のレイアウトを開き、図面の縮尺で重なりを確認します。必要なら地図画面へ戻って調整し、レイアウトを更新します。
5. プロジェクトを保存します（Ctrl+S）。

ラベル位置はQGISの補助記憶を使用し、プロジェクト保存時に `.qgz` 内へまとめて保存されます。
他の人へ渡すときは、`.qgz` だけでなく **`qgs` フォルダを丸ごと**渡してください。
ラベル位置の修正ではGeoPackageの編集モードを開始する必要はありません。
線や点の形状・属性そのものを編集した場合は、別途レイヤの編集内容を保存してください。
このデータは複数のQGZで共有されるため、形状・属性の変更は他の図面にも影響します。

## 周囲図・排水図・全体図の画像を置き換える

1. `shui_1.qgz`、`haisui_1.qgz`、`mix.qgz` など、対象のQGZを開きます。
2. 同名のレイアウトを開き、「画像としてエクスポート」を選択します。
3. PNG形式、解像度 **300 dpi**、ページ全体 **150 × 150 mm** のまま出力します。
   「内容に合わせて切り抜く」は使用しません。画像は約1772 × 1772ピクセルです。
4. 出力フォルダの `asset/shui_1_map.png`、`asset/haisui_1_map.png`、`asset/mix_map.png` など、対応する画像を上書きします。
5. ブラウザで `index.html` を再読み込みして確認します。古い画像が残る場合はCtrl+F5を押します。

登録順の番号ごとに別のQGZを作成します。正確な対応先は、生成した操作説明末尾の一覧を確認してください。

## 位置図をPDFにする

1. `location.qgz` を開き、`location` レイアウトを開きます。
2. 背景・縮尺・方位記号・文字を確認します。
3. 「PDFとしてエクスポート」を選び、出力フォルダの「林小班名 - 位置図.pdf」に保存します。

位置図には生成時に表示対象だった背景レイヤも保存します。
ローカルのラスター背景は `background_*.tif`、ベクター背景はGeoPackageにコピーします。
XYZ・WMSなどの配信背景は接続設定を保存するため、再表示時にもネット接続や元の認証が必要です。
背景を表示した状態でプラグインを実行してください。

## 再生成する場合

プラグインで同じ出力先に再生成すると、QGZ・GeoPackage・画像・PDFも上書きされます。
手動調整を残す場合は、先に出力フォルダ全体を別の場所へコピーしてください。
生成前には対象QGZを閉じてください。GeoPackageを他のQGIS画面で編集中の場合、上書きに失敗することがあります。

地図画像だけを差し替えても、HTML内の座標表・面積・延長は再計算されません。
測点や形状を変更した場合はプラグインで図面一式を再生成してください。
